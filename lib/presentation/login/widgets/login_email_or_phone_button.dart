import 'package:flutter/material.dart';
import 'package:rcm/shared/basic_button.dart';

class LoginEmailOrPhoneButton extends StatelessWidget {
  const LoginEmailOrPhoneButton({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return BasicButton(onPressed: () => {}, label: "ĐĂNG NHẬP");
  }
}
